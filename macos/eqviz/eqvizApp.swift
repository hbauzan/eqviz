import SwiftUI

@main
struct EqvizApp: App {
    var body: some Scene {
        Window("eqviz", id: "main") {
            Color.black
                .ignoresSafeArea()
        }
        .defaultSize(width: 800, height: 240)
    }
}
