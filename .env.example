# eqviz local runtime. Copy to `.env` and fill values. Never commit `.env`.

LOG_LEVEL=INFO

# Audio capture (used once the visualizer loop exists)
# AUDIO_DEVICE=
# SAMPLE_RATE=44100
# FFT_SIZE=2048
